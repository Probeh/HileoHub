import { ITechnology } from '@models/technology'

/* Firebase, Azure, AWS, Github, Google, InVision, Zeplin, Sketch, Monday, Slack */
export interface ICloudService extends ITechnology {
  // ======================================= //

  // ======================================= //
}
