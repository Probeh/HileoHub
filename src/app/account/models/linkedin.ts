import { BaseModel, IBaseModel } from '@models/@model'

export interface ILinkedinProfile extends IBaseModel {
  // ======================================= //

  // ======================================= //
}
export class LinkedinProfile extends BaseModel {
  // ======================================= //
  constructor(model?: ILinkedinProfile) {
    super(model);
  }
  // ======================================= //
}
