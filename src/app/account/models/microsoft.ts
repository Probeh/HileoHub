import { BaseModel, IBaseModel } from '@models/@model'

export interface IMicrosoftProfile extends IBaseModel {
  // ======================================= //

  // ======================================= //
}
export class MicrosoftProfile extends BaseModel {
  // ======================================= //
  constructor(model?: IMicrosoftProfile) {
    super(model);
  }
  // ======================================= //
}
