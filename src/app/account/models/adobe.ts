import { BaseModel, IBaseModel } from '@models/@model'

export interface IAdobeProfile extends IBaseModel {
  // ======================================= //

  // ======================================= //
}
export class AdobeProfile extends BaseModel {
  // ======================================= //
  constructor(model?: IAdobeProfile) {
    super(model);
  }
  // ======================================= //
}
