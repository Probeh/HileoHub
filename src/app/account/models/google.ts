import { BaseModel, IBaseModel } from '@models/@model'

export interface IGoogleProfile extends IBaseModel {
  // ======================================= //

  // ======================================= //
}
export class GoogleProfile extends BaseModel {
  // ======================================= //
  constructor(model?: IGoogleProfile) {
    super(model);
  }
  // ======================================= //
}
